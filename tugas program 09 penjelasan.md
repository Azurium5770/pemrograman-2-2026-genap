Ambil dan tampilkan data suhu dan kelembapan dari layanan API pada openweather melalui web nya openweather map.

petujuk
API pada web open weather.
pakai %20 untuk menggantikan spasi pada URL.

note
untuk bisa menggunakan API harus daftar lalu login oada web openweather dan click username dah pilih my API key, key terletak pada kolom key yang bisa di copy paste.

import modul yang dibutuhkan
input API key
input kota
input negara
input URL
input data dengan menggunakan request ke URL dengan format data yang di ambil menggunakan format JSON
loop if-else dan except error untuk proses
lalu print